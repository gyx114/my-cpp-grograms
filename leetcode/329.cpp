#include <bits/stdc++.h>
using namespace std;
class Solution
{
public:
    int longestIncreasingPath(vector<vector<int>> &nums)
    {
        int m = nums.size();
        int n = nums[0].size();
        vector<vector<int>> nexts(m * n);
        for (int i = 0; i < m; i++)
        {
            for (int j = 0; j < n; j++)
            {
                int idx = i * m + j;
                // 左
            }
        }
    }
};
